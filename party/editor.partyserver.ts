import { YServer } from "y-partyserver";

/**
 * Placeholder PartyServer implementation for the collaborative editor.
 * The full port from PartyKit will populate lifecycle hooks in bear-fit-10.
 */
export class EditorPartyServer extends YServer {}
